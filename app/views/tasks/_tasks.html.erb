  <h1>タスク一覧</h1>
  <ul class="list-unstyled">
  <% tasks.each do |task| %>
      <div>
        <%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %>
      </div>
      
  <% end %>
  <%= paginate tasks %>
</ul>